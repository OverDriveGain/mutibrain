<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>AI Assistant</string>
    <key>UILaunchScreen</key>
    <dict/>

    <!-- Keep recording the mic while backgrounded / locked. -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
    </array>

    <key>NSMicrophoneUsageDescription</key>
    <string>The assistant streams your microphone so it can listen and respond.</string>

    <!-- Allow ws:// (cleartext) to a LAN dev server. Tighten for production. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>
</dict>
</plist>
